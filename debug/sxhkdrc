alt + Return
	urxvt

alt + d
	rofi -show run

alt + {h, j, k, l}
	wmevent go {left, down, up, right}

alt + shift + {h, j, k, l}
	wmevent move {left, down, up, right}

alt + {e, w}
	wmevent {focusnext, focusprev}

alt + q
	wmevent kill

alt + m
	wmevent zoom

alt + shift + e
	wmevent exit
